apiVersion: v1
kind: Service
metadata:
  name: {{ include "week3presentation.fullname" . }}
  labels:
    {{- include "week3presentation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "week3presentation.selectorLabels" . | nindent 4 }}
