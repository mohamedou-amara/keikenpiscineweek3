apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "week3presentation.fullname" . }}-test-connection"
  labels:
    {{- include "week3presentation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "week3presentation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
